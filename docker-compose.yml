services:
  research:
    build:
      context: .
      dockerfile: docker/research/Dockerfile
    container_name: bitcoin-prompt-research
    env_file:
      - .env
    volumes:
      - .:/app
    working_dir: /app
    command: ["uv", "run", "python", "scripts/run_final_analysis.py"]
